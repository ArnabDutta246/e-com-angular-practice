export interface Product {
    id: string;
    image: string;
    title: string;
    price: string;
    description: string;
  }

export interface PageData{
  id:string;
  title:string;
  template:string;
}